# Main environment file with load directives
APP_NAME=TestApp
APP_ENV=testing

# Load modular configurations
load(.env.database)
load(.env.logger)

# Optional includes
load?(.env.optional)
load?(.env.nonexistent)

# Variable after includes (should override if same key)
APP_DEBUG=true
